# AudioModem

Audio modem challenge workspace containing Python scripts, channel data, WAV inputs, recovered files, and analysis outputs.

Large generated sample exports and OFDM stage outputs are excluded from Git because several files exceed GitHub's normal 100 MB per-file limit.
